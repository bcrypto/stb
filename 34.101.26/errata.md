# Поправка к официальной редакции

| В каком месте | Напечатано | Должно быть |
|---------------|------------|-------------|
|Пункт 5.1, абзац 2|OCSP позволяет приложениям определять состояние заданного сертификата. С помощью OCSP можно получить своевременную и более полную информацию о статусе сертификата, чем с помощью СОС.|OCSP позволяет приложениям определять состояния заданных сертификатов. С помощью OCSP можно получить своевременную и более полную информацию о статусе одного или нескольких сертификатов, чем с помощью СОС.|
|Пункт 5.3, абзац 3|– названия OCSP сервера;|– идентификатора OCSP сервера;<br/>– времени создания ответа;|
|Пункт 5.3, абзац 6|Данный ответ означает, что сертификат не отозван, но не означает, что сертификат когда-либо выдавался или что время, в которое был дан ответ, находится в пределах срока действия сертификата.|Данный ответ означает, что сертификат с запрашиваемым серийным номером не был отозван в течение его срока действия, но не означает, что сертификат когда-либо выдавался или что время, в которое был дан ответ, находится в пределах срока действия сертификата.|
|Пункт 5.3, абзац 7|Значение `revoked` означает, что сертификат был отозван навсегда или что его действие было временно приостановлено.|Значение `revoked` означает, что сертификат был отозван навсегда или что его действие было временно приостановлено. Этот статус также МОЖЕТ возвращаться, если у УЦ отсутствует запись о выдаче сертификата с серийным номером, указанным в запросе (далее — невыпущенный сертификат).|
|Пункт 5.3, абзац 8|Значение `unknown` означает, что серверу ничего не известно про запрашиваемый сертификат.|Значение `unknown` означает, что серверу ничего не известно про запрашиваемый сертификат. Обычно данное значение возвращается тогда, когда в запросе указан УЦ, не обслуживаемый сервером.<br/>Примечание. Статус `revoked` означает, что сертификат с запрашиваемым серийным номером должен быть отклонен, а статус `unknown` означает, что статус не может быть определен сервером и клиент для определения статуса может попытаться использовать другие источники информации о статусе (например, СОС). Это делает возврат статуса `revoked` подходящим для невыпущенных сертификатов, когда требуется отклонить сертификат, а не уточнять статус сертификата из других источников информации. Статус `revoked` является необязательным для обратной совместимости.`|
|Пункт 5.3, после 9 абзаца, добавить абзац||В контейнере `SingleResponse` для невыпущенного сертификата:<br/>– в компоненте `revocationReason` ДОЛЖНА быть указана причина отзыва `certificateHold`;<br/>– в компоненте `revocationTime` ДОЛЖНА быть установлена дата 01.01.1970;<br/>– не должны включаться расширения `CrlID` (п. 6.3.2) и `CRLEntry` (п. 6.3.5).|
|Пункт 5.4, последний абзац|Ответ `unauthorized` означает, что клиент не имеет полномочий делать запрос на данный сервер OCSP.|Ответ `unauthorized` означает, что клиент не имеет полномочий делать запрос на данный сервер OCSP или у сервера нет возможности дать достоверный ответ.|
|Пункт 5.5, абзац 1|Ответ может содержать три метки времени:<br/>– `thisUpdate` — время, когда точно известно, что текущий статус сертификата верен;<br/>– `nextUpdate` — время, когда будет доступна новая информация о статусе сертификата;<br/> – `producedAt` — время, когда сервер OCSP подписал данный ответ.|Ответ может содержать четыре метки времени:<br/>– `thisUpdate` — время, когда точно известно, что текущий статус сертификата верен;<br/>– `nextUpdate` — время, когда будет доступна новая информация о статусе сертификата;<br/>– `producedAt` — время, когда сервер OCSP подписал данный ответ;<br/>– `revocationTime` — время, когда проверяемый сертификат был отозван или его действие было временно приостановлено.|
|Пункт 5.10.2, абзац 1|– подписывающая сторона в настоящее время уполномочена подписывать ответ;|– подписывающая сторона в настоящее время уполномочена давать ответ по сертификату в запросе;|
|Пункт 6.2.1, абзац 7|– `certs` — цепочка сертификатов, демонстрирующая действительность открытого ключа ЭЦП.|– `certs` — цепочка сертификатов, демонстрирующая действительность открытого ключа ЭЦП. Сервер МОЖЕТ включать в компонент `certs` сертификаты, которые помогут клиенту выполнить проверку подписи. Если ни один сертификат не включен, то компонент `certs` ДОЛЖЕН быть опущен.|
|Пункт 6.2.1, абзац 13|Ответы, в которых значение компонента `nextUpdate` не задано, эквивалентны СОС, в которых компонент `nextUpdate` также не задан (см. 5.5);|Если сервер не задает значение компонента `nextUpdate`, то тем самым он сообщает, что актуальная информация об отзыве доступна в любое время;|
|Пункт 6.2.2, абзац 1|Данный сертификат ДОЛЖЕН выдаваться напрямую УЦ, который выпустил проверяемый сертификат.|Данный сертификат ДОЛЖЕН выдаваться напрямую УЦ, который выпустил проверяемый сертификат. При выпуске сертификата делегирования УЦ ДОЛЖЕН использовать тот же личный ключ, что и при выпуске сертификата, проверяемого на отзыв. Системы, полагающиеся на ответы OCSP, ДОЛЖНЫ признавать сертификат делегирования, только если он подписан на том же личном ключе, что и сертификат, проверяемый на отзыв.<br/>Примечание. Для обратной совместимости не запрещается выдавать сертификат для сервера OCSP, подписанный личным ключом, отличным от используемого для выпуска сертификата, проверяемого на отзыв. Однако такая практика должна применяться в исключительных случаях, поскольку клиенты не обязаны распознавать сервер с таким сертификатом как уполномоченный.|
| Пункт 6.2.2, абзац 2 | Клиенты OCSP ДОЛЖНЫ определять и использовать значение поля `id-ad-ocspSigning`, как описано выше. | Клиенты OCSP ДОЛЖНЫ определять и использовать значение поля `id-kp-ocspSigning`, как описано выше. |
| Пункт 6.2.2, абзац 2 | - содержит значение `id-ad-ocspSigning` в расширении `ExtendedKeyUsage` и выпущен УЦ, который выдал проверяемый сертификат. | - содержит значение `id-kp-ocspSigning` в расширении `ExtendedKeyUsage` и выпущен УЦ, который выдал проверяемый сертификат. |
|Приложение Б, модуль АСН.1||После строки <br/>`UnknownInfo ::= NULL`<br/>добавить<br/>&nbsp;&nbsp; `CrlID ::= SEQUENCE {`<br/>&nbsp;&nbsp;&nbsp;&nbsp;`crlUrl [0] EXPLICIT IA5String OPTIONAL ,`<br/>&nbsp;&nbsp;&nbsp;&nbsp;`crlNum [1] EXPLICIT INTEGER OPTIONAL ,`<br/>&nbsp;&nbsp;&nbsp;&nbsp;`crlTime [2] EXPLICIT GeneralizedTime OPTIONAL`<br/>&nbsp;&nbsp; ` }`|
